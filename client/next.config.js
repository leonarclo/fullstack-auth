/** @type {import('next').NextConfig} */
const nextConfig = {};

module.exports = {
  nextConfig,
  images: {
    domains: ["i.ytimg.com"], // Certifique-se de que o hostname correto está aqui
  },
};
